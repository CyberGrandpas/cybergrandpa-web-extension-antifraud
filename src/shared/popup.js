// Shared popup script logic
const browserAPI = chrome; // Will be replaced with browser-specific API during build

// Add your popup logic here
document.addEventListener('DOMContentLoaded', () => {
  // Initialize popup
  console.log('Popup loaded');
});
